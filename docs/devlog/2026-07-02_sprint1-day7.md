# Dev Log — Sprint 1, Day 7: Integration & Review

**Date:** 2 July 2026
**Sprint:** Sprint 1 — Docker & Project Scaffold (`SPRINT_CHECKLIST.md`)
**Scope covered today:** Day 7 tasks ("Integration & review") — **final day of Sprint 1**
**Status:** ✅ Complete — Sprint 1 CLOSED
**Depends on:** Day 1-2 (Docker foundation), Day 3-4 (Backend scaffold), Day 5-6 (Frontend scaffold)

---

## 1. Objective

Per `SPRINT_CHECKLIST.md` Sprint 1, Day 7: prove the full stack — not
just the individual pieces built on Day 1-6 — boots together from a
completely clean state with zero manual intervention, then close out
Sprint 1 with a commit and version tag.

This is the sprint's actual Definition of Done: individual services
working in isolation (Day 1-2, 3-4, 5-6) is necessary but not
sufficient — Day 7 is where that's proven end-to-end.

---

## 2. Verification Steps

### 2.1 Full clean-state reset

```powershell
docker compose down -v
```

Removed all containers and the named `postgres_data` volume — this
guarantees the next `up` is a true cold start, not relying on any
state left over from Day 1-6 testing.

### 2.2 Full stack up

```powershell
docker compose up
```

All 3 services started together in the correct order:
1. `postgres` — booted, healthcheck (`pg_isready`) passed
2. `backend` — waited for `postgres: condition: service_healthy` (per `docker-compose.yml`), then started
3. `frontend` — started after `backend` (per `depends_on`)

No manual steps required between containers.

### 2.3 Backend health check

```powershell
curl http://localhost:3001/health
```
Result: `HTTP/1.1 200 OK`, `{"status":"ok"}` — consistent with Day 3-4.

### 2.4 Frontend reachability

Opened `http://localhost:3000` in browser — landing page rendered,
consistent with Day 5-6.

### 2.5 Result

Full stack confirmed working from a completely clean `docker compose
down -v` → `docker compose up`, with no manual fixes needed. This
matches Sprint 1's Definition of Done exactly.

---

## 3. Sprint 1 — Closing Summary

| Day | Task | Status |
| --- | --- | --- |
| 1-2 | Docker foundation (compose file, Postgres+PostGIS verified) | ✅ Done |
| 3-4 | Backend scaffold (Clean Architecture folders, `/health` endpoint) | ✅ Done |
| 5-6 | Frontend scaffold (Next.js + Tailwind, landing page) | ✅ Done |
| 7 | Integration & review (full clean-state stack verified) | ✅ Done |

**Sprint 1 Definition of Done (from `SPRINT_CHECKLIST.md`):**
> Fresh clone → `docker compose up` → both health checks pass, no
> manual DB/env fixing needed.

✅ **Met.**

### Issues encountered across the whole sprint (rolled up from Day 1-2, 3-4, 5-6 logs)

| Day | Issue | Resolution |
| --- | --- | --- |
| 1-2 | Docker not installed on Windows | Installed Docker Desktop + WSL2 |
| 1-2 | `postgis_version()` not found | Was connecting to wrong DB (`postgres` instead of `geofinance`) — fixed by adding `-d geofinance` |
| 3-4 | None | Clean run |
| 5-6 | Odd console message `Unable to add filesystem` | Traced to a browser extension, confirmed harmless via Incognito test |

No unresolved issues remain going into Sprint 2.

---

## 4. Git Milestone

```powershell
git add .
git commit -m "Sprint 1 Day 7: full stack integration verified, Sprint 1 complete"
git push
git tag v0.1.0-setup
git push origin v0.1.0-setup
```

This closes out **v0.1.0 — Project Setup** from the Feature Release
Plan in `ROADMAP.md §8`.

---

## 5. Commands Reference (full-stack, for future sessions)

```powershell
# Full clean-state restart (use when debugging weird state issues)
docker compose down -v
docker compose up

# Normal day-to-day start/stop (keeps data)
docker compose up
docker compose down

# Check everything is healthy
docker ps
curl http://localhost:3001/health
# then open http://localhost:3000 in a browser
```

---

## 6. Next Session — Sprint 2: PostGIS, GeoServer & Seed Data (Days 8-14)

**Goal:** Database schema live via migrations, GeoServer serving one
WMS layer, seed data loaded into the running database.

- [ ] Day 8-9: Choose migration tool, write migrations for all 8 tables in `docs/03_DATABASE.md §2`, add GiST indexes
- [ ] Day 10: Update `database/seed-data/generate_seed_data.py` to generate UUID `id` alongside the existing string `code`
- [ ] Day 11: Load seed data via `import.sh`, verify row counts
- [ ] Day 12-13: Add GeoServer to `docker-compose.yml`, connect to Postgres, publish `branches` as a WMS layer
- [ ] Day 14: Full review, commit, tag `v0.1.1-data`

---

*Log format: one entry per working session, filed under
`docs/devlog/`. Update `SPRINT_CHECKLIST.md` checkboxes in the same
commit as each dev log entry. This entry closes the Sprint 1 log
series (Day 1-2, 3-4, 5-6, 7).*
